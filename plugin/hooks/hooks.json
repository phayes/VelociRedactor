{
  "description": "Deny reading, or searching, files the project's veloci.yml protects when its agent section sets enforce: true, pointing the agent at veloci redact or grep instead.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/veloci\" agent hook",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
